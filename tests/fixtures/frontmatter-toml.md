+++
title = "Front matter, the TOML kind"
draft = false

[taxonomies]
tags = ["metadata", "hugo", "zola"]
+++

# TOML front matter

The `+++` block above is the Hugo/Zola dialect. It must render exactly as the YAML
fixture's does — one collapsed *Frontmatter* disclosure — differing only in the language
the code block is highlighted as.
